# 悦读小灯塔 · 并行云部署教程

> 针对并行云（paratera.com）GPU 服务器的完整部署指南。
> 注意：你的项目不需要 GPU，但并行云的 CPU 实例同样可以用，本教程通用。

---

## 准备工作

你需要：
- 并行云账号（已购买实例）
- 本地电脑（Windows/Mac）
- 项目代码（本地或 GitHub）

---

## 第一步：连接并行云服务器

### 获取连接信息

1. 登录并行云控制台：https://www.paratera.com
2. 进入"我的实例"，找到你购买的实例
3. 记下：
   - **公网 IP**（或 SSH 连接地址）
   - **端口号**（并行云默认 SSH 端口通常不是 22，注意查看）
   - **用户名**（通常是 `root` 或 `ubuntu`）
   - **密码**（购买时设置的，或控制台重置）

### Windows 用 MobaXterm 连接

1. 下载 [MobaXterm](https://mobaxterm.mobatek.net/download.html) 免费版
2. 打开后点击 **Session → SSH**
3. 填写：
   - Remote host：你的服务器 IP
   - 勾选 "Specify username"，填用户名（如 `root`）
   - Port：填并行云给的端口号（**不一定是 22**，在控制台查看）
4. 点 OK，输入密码

### Mac / Linux 连接

```bash
ssh -p 端口号 root@你的服务器IP
```

> 连上后看到命令行就成功了，接下来所有命令都在这里输入。

---

## 第二步：安装 Docker

并行云的 Ubuntu 镜像通常是干净的，需要手动安装 Docker。

```bash
# 更新系统包
apt update && apt upgrade -y

# 一键安装 Docker
curl -fsSL https://get.docker.com | sh

# 安装 Docker Compose 插件
apt install docker-compose-plugin -y

# 验证安装
docker --version
docker compose version
```

看到版本号输出就说明安装成功。

---

## 第三步：上传项目代码

### 方法一：Git 克隆（推荐，代码在 GitHub/Gitee 上）

```bash
apt install git -y
git clone https://github.com/你的用户名/你的仓库名.git
cd 你的仓库名
```

### 方法二：用 MobaXterm 直接上传

MobaXterm 左侧有文件管理面板，直接把项目文件夹拖进去，上传到 `/root/` 目录下。

上传完成后：
```bash
cd /root/项目文件夹名
```

---

## 第四步：配置后端环境变量

```bash
cd backend
cp .env.example .env
nano .env
```

修改以下关键配置：

```dotenv
# 关闭调试模式
DEBUG=False

# 生成密钥（新开一个终端窗口运行这个命令，复制输出结果）
# python3 -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=粘贴你生成的密钥

# 填写你的服务器公网 IP（或域名）
ALLOWED_ORIGINS=http://你的服务器IP

# AI 功能（可选，不填则用规则型回复）
AI_API_KEY=
AI_API_BASE_URL=https://api.openai.com/v1
AI_MODEL=gpt-4o-mini
```

**nano 编辑器操作：**
- 方向键移动光标，直接输入修改
- 保存退出：`Ctrl+X` → `Y` → 回车

生成密钥的命令：
```bash
python3 -c "import secrets; print(secrets.token_hex(32))"
```

回到项目根目录：
```bash
cd ..
```

---

## 第五步：开放并行云防火墙端口

**这一步很关键，并行云默认只开放 SSH 端口。**

1. 登录并行云控制台
2. 找到你的实例，点击"安全组"或"防火墙"
3. 添加入站规则：

| 端口 | 协议 | 用途 |
|------|------|------|
| 80   | TCP  | 前端页面 |
| 8000 | TCP  | 后端 API |

> 如果找不到安全组，在实例详情页找"网络"或"端口映射"相关设置。

---

## 第六步：启动项目

确保你在项目根目录（能看到 `docker-compose.yml`）：

```bash
ls
# 应该能看到 docker-compose.yml、backend/、frontend/ 等
```

启动所有服务：

```bash
docker compose up -d --build
```

第一次构建需要 **5-15 分钟**（下载依赖），耐心等待。

看到这样的输出就成功了：
```
✔ Container dyslexia-backend   Started
✔ Container dyslexia-frontend  Started
```

验证运行状态：
```bash
docker compose ps
```

两个服务都是 `running` 就 OK。

---

## 第七步：验证部署

在浏览器里访问：

- **前端页面**：`http://你的服务器IP`
- **后端健康检查**：`http://你的服务器IP:8000/health`

后端应该返回：`{"status":"healthy"}`

---

## 第八步：修改前端 API 地址

打开 `frontend/src/api/index.js`，找到 `getBaseUrl` 函数，在小程序条件编译块里填入你的服务器地址：

```javascript
export const getBaseUrl = () => {
  const configured = import.meta.env.VITE_API_BASE_URL || '';
  if (configured) return configured;

  // #ifdef MP-WEIXIN
  return 'http://你的服务器IP:8000';  // ← 改这里
  // #endif

  if (typeof window !== 'undefined') {
    const host = window.location.hostname;
    if (host !== 'localhost' && host !== '127.0.0.1') {
      return `http://${host}:8000`;
    }
  }
  return '';
};
```

修改后重新构建前端容器：
```bash
docker compose up -d --build frontend
```

---

## 并行云特有注意事项

### 按量计费 → 记得关机！

并行云按小时计费，**不用的时候一定要在控制台停止实例**，否则持续扣费。

部署完测试好之后，如果暂时不用：
1. 先停止 Docker 服务：`docker compose down`
2. 再去控制台停止实例

### 数据持久化

Docker 数据（数据库、上传文件）存在 Docker Volume 里，**停止实例不会丢失数据**，但如果你在控制台"销毁实例"，数据会一起删除。

重要数据定期备份：
```bash
# 备份数据库
docker compose exec backend python -c "import shutil; shutil.copy('/app/data/dyslexia.db', '/app/data/dyslexia_backup.db')"

# 把备份文件下载到本地（用 MobaXterm 文件管理器拖出来）
```

### 实例重启后自动恢复

Docker 容器已配置 `restart: unless-stopped`，实例重启后会自动重新启动服务，无需手动操作。

---

## 常用维护命令

```bash
# 查看服务状态
docker compose ps

# 查看后端日志（出问题时看这里）
docker compose logs -f backend

# 重启后端
docker compose restart backend

# 停止所有服务（省钱时用）
docker compose down

# 更新代码后重新部署
git pull
docker compose up -d --build
```

---

## 常见问题

### 浏览器访问不了

1. 检查并行云控制台安全组，80 和 8000 端口是否已开放
2. 检查服务是否在运行：`docker compose ps`
3. 查看日志：`docker compose logs backend`

### 构建时内存不足

并行云实例内存较大，一般不会有这个问题。如果遇到：
```bash
# 创建交换空间
fallocate -l 2G /swapfile
chmod 600 /swapfile
mkswap /swapfile
swapon /swapfile
```

### SSH 连接超时

并行云实例有时会有空闲超时断开的问题，在 MobaXterm 里设置 keepalive：
Session → SSH → Advanced → 勾选 "SSH keepalive"

---

## 微信小程序上线额外要求

如果你要上线微信小程序（不只是 H5），还需要：

1. **域名 + HTTPS**：微信小程序强制要求 HTTPS，不能用裸 IP
   - 购买一个域名（约 ¥50/年）
   - 把域名解析到并行云服务器 IP
   - 申请免费 SSL 证书（Let's Encrypt）

2. **在微信后台配置合法域名**：
   - 登录微信公众平台 → 开发 → 开发管理 → 开发设置
   - 在"服务器域名"里添加你的域名

3. **填写微信 AppID**：
   - `frontend/src/manifest.json` 里的 `mp-weixin.appid` 改为真实 AppID
   - `backend/.env` 里填写 `WX_APPID` 和 `WX_SECRET`

> 如果只是先测试 H5 版本，用 IP + HTTP 就够了，不需要域名。

---

## 部署完成检查清单

- [ ] `http://你的服务器IP` 能打开前端页面
- [ ] `http://你的服务器IP:8000/health` 返回 `{"status":"healthy"}`
- [ ] 能正常注册账号
- [ ] 能创建儿童档案
- [ ] 能进入游戏页面

全部打勾，部署成功！
