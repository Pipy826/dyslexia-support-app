# 悦读小灯塔 · 新手部署教程

> 完全零基础，一步一步跟着做，大约 30 分钟完成部署。

---

## 你需要准备什么

| 需要 | 说明 | 费用 |
|------|------|------|
| 一台电脑 | Windows / Mac 都可以 | 你已有 |
| 一台云服务器 | 推荐腾讯云/阿里云轻量应用服务器 | 约 ¥50/月 |
| 一个域名（可选） | 没有域名也能用 IP 访问 | 约 ¥50/年 |

---

## 第一部分：购买并连接服务器

### 第 1 步：购买云服务器

推荐新手选 **腾讯云轻量应用服务器**（便宜、简单）：

1. 打开 https://cloud.tencent.com/product/lighthouse
2. 点击"立即选购"
3. 配置选择：
   - 地域：选离你近的（如上海）
   - 镜像：选 **Ubuntu 22.04**
   - 套餐：2核2G（够用）
4. 购买完成后，记下你的 **服务器公网 IP**（如 `123.45.67.89`）

### 第 2 步：连接服务器

**Windows 用户：**

1. 下载并安装 [MobaXterm](https://mobaxterm.mobatek.net/download.html)（免费版即可）
2. 打开 MobaXterm，点击左上角 "Session"
3. 选择 "SSH"
4. Remote host 填你的服务器 IP
5. Username 填 `ubuntu`（腾讯云默认）
6. 点击 OK，输入密码（购买时设置的）

**Mac 用户：**

打开终端，输入：
```bash
ssh ubuntu@你的服务器IP
```
输入密码即可。

> 连接成功后，你会看到一个黑色命令行窗口，接下来所有命令都在这里输入。

---

## 第二部分：在服务器上安装必要软件

> 以下命令全部在服务器的命令行里输入，每行输入完按回车。

### 第 3 步：更新系统

```bash
sudo apt update && sudo apt upgrade -y
```

等待完成（约 1-2 分钟）。

### 第 4 步：安装 Docker

Docker 是一个让程序"打包运行"的工具，我们用它来运行项目。

```bash
# 一键安装 Docker
curl -fsSL https://get.docker.com | sudo sh

# 让当前用户可以使用 Docker（不用每次加 sudo）
sudo usermod -aG docker $USER

# 重新加载权限（重要！）
newgrp docker

# 验证安装成功
docker --version
```

看到类似 `Docker version 24.x.x` 就说明安装成功了。

### 第 5 步：安装 Docker Compose

```bash
# 更新系统包
apt update && apt upgrade -y

# 一键安装 Docker
curl -fsSL https://get.docker.com | sh

# 安装 Docker Compose 插件
apt install docker-compose-plugin -y

# 验证安装
docker --version
docker compose version
```
网络问题导致 Docker 官方脚本下载失败。用国内镜像安装：

# 使用阿里云镜像安装 Docker
apt install -y docker.io

# 启动 Docker
systemctl start docker
systemctl enable docker

# 验证
docker --version
然后安装 docker-compose（独立版本）：

# 下载 docker-compose（国内镜像）
curl -L "https://github.com/docker/compose/releases/download/v2.24.0/docker-compose-linux-x86_64" -o /usr/local/bin/docker-compose

# 如果 GitHub 下载慢，用这个国内镜像
curl -L "https://mirror.ghproxy.com/https://github.com/docker/compose/releases/download/v2.24.0/docker-compose-linux-x86_64" -o /usr/local/bin/docker-compose

# 添加执行权限
chmod +x /usr/local/bin/docker-compose

# 验证
docker-compose --version

看到版本号就成功了。

---

## 第三部分：上传项目代码

### 第 6 步：把代码上传到服务器

**方法一：用 Git（推荐）**

如果你的代码在 GitHub/Gitee 上：

```bash
# 在服务器上安装 git
sudo apt install git -y

# 克隆你的代码（替换为你的仓库地址）
git clone https://github.com/你的用户名/你的仓库名.git

# 进入项目目录
cd 你的仓库名

# 切换到你的分支（替换成你的分支名）
git checkout 你的分支名

```

**方法二：直接上传文件**

用 MobaXterm 的文件管理器（左侧面板），直接把项目文件夹拖进去，上传到 `/home/ubuntu/` 目录下。

上传完成后：
```bash
cd /home/ubuntu/项目文件夹名
```

---

## 第四部分：配置项目

### 第 7 步：创建后端配置文件

```bash
# 进入 backend 目录
cd backend

# 复制配置模板
cp .env.example .env

# 用编辑器打开配置文件
code .env
```

你会看到一个文本编辑器，需要修改以下几项：

```
# 1. 关闭调试模式（重要！）
DEBUG=False

# 2. 生成并填写密钥（见下方说明）
SECRET_KEY=这里填你生成的密钥

# 3. 填写你的服务器 IP 或域名
ALLOWED_ORIGINS=http://你的服务器IP
```

**生成密钥的方法：**

打开一个新的终端窗口（或者先按 Ctrl+C 退出编辑器），输入：
```bash
python -c "import secrets; print(secrets.token_hex(32))"
```
复制输出的那串字符，粘贴到 `SECRET_KEY=` 后面。

**nano 编辑器操作说明：**
- 用方向键移动光标
- 直接输入文字
- 修改完成后按 `Ctrl + X`，再按 `Y`，再按回车，保存退出

### 第 8 步：回到项目根目录

```bash
cd ..
```

---

## 第五部分：启动项目

### 第 9 步：一键启动

```bash
# 确保在项目根目录（能看到 docker-compose.yml 文件）
ls

# 启动所有服务
docker compose up -d --build
```

第一次运行会下载和构建镜像，需要 **5-10 分钟**，耐心等待。

看到类似这样的输出就说明成功了：
```
✔ Container dyslexia-backend   Started
✔ Container dyslexia-frontend  Started
```

### 第 10 步：验证是否成功

```bash
# 查看运行状态
docker compose ps
```

两个服务都显示 `running` 就成功了。

在浏览器里访问：
- 前端页面：`http://你的服务器IP`
- 后端接口：`http://你的服务器IP:8000/health`（应该显示 `{"status":"healthy"}`）

---

## 第六部分：开放防火墙端口

如果访问不了，需要在云服务商控制台开放端口。

### 第 11 步：腾讯云开放端口

1. 登录腾讯云控制台
2. 找到你的轻量应用服务器
3. 点击"防火墙"
4. 添加规则：
   - 端口 `80`（HTTP，前端）
   - 端口 `8000`（后端 API）
   - 协议都选 TCP
   - 来源选"所有 IP"

### 第 11 步（阿里云版）：

1. 登录阿里云控制台
2. 找到你的 ECS 实例
3. 点击"安全组" → "配置规则"
4. 添加入方向规则：端口 `80` 和 `8000`

---

## 第七部分：让前端连接到你的后端

### 第 12 步：修改前端 API 地址

打开项目里的 `frontend/src/api/index.js` 文件，找到这一行：

```javascript
return 'https://your-api-domain.com';  // ← 云打包前替换为真实后端地址
```

把 `https://your-api-domain.com` 改成你的服务器地址：

```javascript
return 'http://你的服务器IP:8000';
```

修改完后，重新构建前端：

```bash
docker compose up -d --build frontend
```

---

## 第八部分：常用维护命令

学会这几个命令，日常维护就够用了：

```bash
# 查看所有服务状态
docker compose ps

# 查看后端日志（出问题时看这里）
docker compose logs backend

# 查看前端日志
docker compose logs frontend

# 重启某个服务
docker compose restart backend

# 停止所有服务
docker compose down

# 更新代码后重新部署
git pull
docker compose up -d --build
```

---

## 常见问题

### 问题 1：访问页面显示"无法连接"

**原因**：防火墙没开放端口

**解决**：按第 11 步开放 80 和 8000 端口

---

### 问题 2：页面能打开但登录报错"网络请求失败"

**原因**：前端没有正确连接到后端

**解决**：检查 `frontend/src/api/index.js` 里的后端地址是否正确，然后重新构建

---

### 问题 3：`docker compose up` 报错

**原因**：可能是 `.env` 文件没创建，或者配置有误

**解决**：
```bash
# 检查 .env 文件是否存在
ls backend/.env

# 查看详细错误日志
docker compose logs backend
```

---

### 问题 4：服务器内存不足，构建失败

**原因**：2G 内存在构建时可能不够

**解决**：
```bash
# 创建 2G 交换空间（临时增加内存）
sudo fallocate -l 2G /swapfile
sudo chmod 600 /swapfile
sudo mkswap /swapfile
sudo swapon /swapfile
```
然后重新运行 `docker compose up -d --build`

---

## 部署完成后的检查清单

- [ ] 浏览器能打开 `http://你的服务器IP`
- [ ] 能正常注册账号
- [ ] 能创建儿童档案
- [ ] 能进入游戏页面
- [ ] 后端健康检查正常：`http://你的服务器IP:8000/health`

全部打勾，恭喜你部署成功！🎉

---

## 下一步（可选）

部署成功后，如果想进一步完善：

1. **绑定域名**：在域名服务商把域名解析到服务器 IP，然后修改 Nginx 配置
2. **开启 HTTPS**：用 Let's Encrypt 申请免费 SSL 证书（详见 DEPLOYMENT_GUIDE.md）
3. **配置 AI 功能**：在 `backend/.env` 里填写 `AI_API_KEY`
4. **配置短信验证码**：在 `backend/.env` 里填写短信服务商配置

遇到问题随时查看日志：`docker compose logs -f backend`
