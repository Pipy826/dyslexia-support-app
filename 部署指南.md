# 悦读灯塔 · 完整部署指南

> 目标：后端部署到云服务器 → 前端用 HBuilderX 打包成 App → 生成二维码扫码进入

---

## 整体架构

```
用户扫描二维码
      ↓
下载/打开 APK（Android App）
      ↓
App 通过网络请求
      ↓
云服务器（FastAPI 后端 + PostgreSQL）
```

---

## 第一步：准备云服务器

### 推荐配置

| 项目 | 要求 |
|------|------|
| 操作系统 | Ubuntu 22.04 LTS（推荐）|
| CPU | 2核+ |
| 内存 | 2GB+（推荐 4GB）|
| 硬盘 | 20GB+ |

### 开放防火墙端口

在云服务商控制台的**安全组**中开放：

| 端口 | 用途 |
|------|------|
| 22 | SSH 登录 |
| 80 | HTTP（Nginx）|
| 8000 | 后端 API（App 直连）|

---

## 第二步：部署后端到服务器

### 2.1 连接服务器

```bash
ssh root@你的服务器IP
```

### 2.2 上传项目代码

**方法一：Git（推荐）**
```bash
mkdir -p /opt/dyslexia-app
cd /opt/dyslexia-app
git clone https://github.com/你的用户名/你的仓库.git .
```

**方法二：SCP 上传**
```bash
# 在本地电脑执行（Windows 用 WinSCP 或 FileZilla）
scp -r ./backend root@你的服务器IP:/opt/dyslexia-app/
scp docker-compose.prod.yml root@你的服务器IP:/opt/dyslexia-app/
scp nginx.conf root@你的服务器IP:/opt/dyslexia-app/
scp deploy.sh root@你的服务器IP:/opt/dyslexia-app/
```

### 2.3 配置环境变量

```bash
cd /opt/dyslexia-app
cp backend/.env.example backend/.env
nano backend/.env
```

**必须修改的配置项：**

```env
DEBUG=False

# 生成随机密钥（在服务器上执行这条命令获取）：
# python3 -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=你生成的随机密钥

# 数据库密码（自己设一个强密码）
DATABASE_URL=postgresql://dyslexia_user:你的数据库密码@postgres:5432/dyslexia

# 允许 App 访问（填你的服务器公网 IP）
ALLOWED_ORIGINS=http://你的服务器IP:8000,http://你的服务器IP
```

### 2.4 一键启动

```bash
cd /opt/dyslexia-app
chmod +x deploy.sh
DB_PASSWORD=你的数据库密码 ./deploy.sh
```

### 2.5 验证部署

```bash
# 本地测试
curl http://localhost:8000/health
# 返回：{"status":"healthy"}

# 浏览器访问（在你的电脑上）
http://你的服务器IP:8000/health
```

---

## 第三步：配置前端连接后端

打开 `frontend/.env.production`，将 IP 替换为你的真实服务器公网 IP：

```env
VUE_APP_API_BASE_URL=http://123.456.789.0:8000
```

> 注意：填写公网 IP，不能用 localhost

---

## 第四步：HBuilderX 云打包 App

### 4.1 安装 HBuilderX

下载正式版：https://www.dcloud.io/hbuilderx.html
注册 DCloud 账号：https://dev.dcloud.net.cn/

### 4.2 导入项目

HBuilderX 菜单：**文件** → **导入** → **从本地目录导入** → 选择 `frontend` 文件夹

### 4.3 生成签名证书（首次打包）

1. 菜单：**发行** → **原生 App-云打包**
2. Android 签名证书栏 → 点击 **"生成证书"**
3. 填写别名 `dyslexia`、密码、有效期 `25` 年
4. 保存 `.keystore` 文件（**妥善保管，丢失无法更新 App**）

### 4.4 开始云打包

1. 菜单：**发行** → **原生 App-云打包**
2. 填写 Android 配置（包名已配置为 `com.dyslexia.lighthouse`）
3. 点击 **"打包"**，等待 3-10 分钟
4. 下载 `.apk` 文件

---

## 第五步：生成二维码（扫码进入系统）

### 推荐方案：蒲公英分发平台

蒲公英是国内最常用的 App 内测分发平台，自带二维码：

1. 注册：https://www.pgyer.com/
2. 上传 APK 文件
3. 蒲公英自动生成下载页面和**专属二维码**
4. 用户扫码 → 打开下载页 → 点击安装 → 进入系统

每次更新只需重新上传 APK，二维码链接不变。

### 备选方案：自定义二维码

如果已有域名或固定 IP，可以：
1. 将 APK 上传到阿里云 OSS / 腾讯云 COS，获取直链
2. 在 https://cli.im/ 或 https://qr.io/ 输入下载链接
3. 生成二维码图片，打印或展示

---

## 常见问题

**App 打开后提示"网络请求失败"**
- 检查 `frontend/.env.production` 中的 IP 是否正确
- 检查服务器防火墙是否开放了 8000 端口
- 运行 `docker compose -f docker-compose.prod.yml ps` 确认服务在运行

**后端启动失败**
```bash
docker logs dyslexia-backend --tail 100
```

**数据库连接失败**
```bash
docker logs dyslexia-postgres --tail 50
docker exec dyslexia-backend alembic upgrade head
```

**只更新后端代码（不需要重新打包 App）**
```bash
cd /opt/dyslexia-app
git pull
docker compose -f docker-compose.prod.yml up -d --build backend
```

**更新前端（需要重新云打包）**
1. 修改前端代码
2. 更新 `manifest.json` 中的版本号（`versionName` 和 `versionCode`）
3. 重新 HBuilderX 云打包
4. 上传新 APK 到蒲公英

---

## 快速命令参考

```bash
# 查看所有服务状态
docker compose -f docker-compose.prod.yml ps

# 查看后端日志
docker logs dyslexia-backend -f

# 重启后端
docker compose -f docker-compose.prod.yml restart backend

# 停止所有服务
docker compose -f docker-compose.prod.yml down

# 完全重建（代码更新后）
docker compose -f docker-compose.prod.yml up -d --build
```
