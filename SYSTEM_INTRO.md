# 悦读小灯塔——儿童读写能力发展游戏平台

## 一、项目概述

悦读小灯塔是一款面向家庭使用场景的儿童读写能力发展游戏平台。该系统通过游戏化互动的方式让孩子在轻松有趣的氛围中展示读写相关能力，结合人工智能技术生成能力画像与成长报告，帮助家长了解孩子的读写学习特点，发现成长卡点，获得个性化的成长建议。系统采用前后端分离架构，前端基于UniApp框架开发，支持H5、微信小程序、iOS和Android多端部署，后端采用FastAPI构建，提供完整的RESTful API服务。

本系统的核心价值闭环涵盖五个关键环节：发现卡点、理解特点、获得建议、开展训练和跟踪变化。家长通过系统引导孩子进入游戏后，儿童以游戏形式完成各项能力探索，系统根据行为数据计算各维度得分并生成关注等级，随后自动生成个性化的训练建议和成长方案。家长可以通过AI问答功能深入了解报告含义，获取针对性的指导，并在后续持续跟踪儿童的能力发展变化。整个流程设计充分考虑了家庭使用场景的特点，既保证了专业性，又兼顾了用户体验的友好性。

系统包含三种用户角色：家长端、儿童端和管理员端。家长是主要的决策者和使用者，负责管理儿童档案、发起能力探索、查看报告、咨询AI和管理训练计划；儿童是参与游戏和训练的主体，通过游戏化的界面完成各项任务，获得即时反馈和奖励激励；管理员功能目前仍在规划中，未来将提供用户管理、数据统计和内容管理等扩展能力。需要特别说明的是，本系统仅作为家庭参考工具，评估结果不构成医学诊断，如结果显示需要更多关注，建议家长及时咨询专业医疗机构或持证评估师进行正式评估。

## 二、系统架构与技术栈

### 2.1 整体架构

系统采用经典的前后端分离架构，通过RESTful API进行数据交互。前端负责用户界面渲染和交互逻辑处理，后端负责业务逻辑处理、数据持久化和AI服务集成。数据库采用关系型数据库，SQLite用于开发环境，PostgreSQL用于生产环境。系统支持容器化部署，提供了完整的Docker Compose配置，可实现一键启动开发环境或生产部署。

前端应用基于UniApp框架构建，该框架基于Vue 3语法体系，支持一套代码编译到多个平台，包括H5网页、微信小程序、iOS应用和Android应用。前端项目使用Vite 5作为构建工具，提供快速的开发服务器启动和热更新能力。前端代码组织采用模块化结构，将页面组件、API接口封装、公共组件和工具函数分别归类，便于维护和扩展。

后端应用基于FastAPI框架构建，该框架是Python生态中最现代、最高性能的Web框架之一，原生支持异步编程，提供了自动生成API文档的能力。后端采用SQLAlchemy 2.0作为ORM层，通过Pydantic v2进行数据验证，使用JWT进行用户认证。系统集成了兼容OpenAI格式的AI服务，支持接入OpenAI、DeepSeek、通义千问等大语言模型服务提供商。

### 2.2 技术栈详情

前端技术栈包括UniApp（Vue 3）、Vite 5、Vue Router、Pinia状态管理和uView UI组件库。UniApp的核心优势在于跨平台编译能力，开发者只需编写一套Vue代码，即可同时生成iOS、Android应用以及各种小程序的安装包。Vite 5作为新一代前端构建工具，利用浏览器原生ES模块支持实现极速的开发服务器启动和热模块替换。状态管理采用Pinia，这是Vue官方推荐的新一代状态管理库，相比Vuex具有更简洁的API和更好的TypeScript支持。

后端技术栈包括FastAPI 0.115.0、Uvicorn、SQLAlchemy 2.0.36、Pydantic v2.9.2、python-jose（JWT认证）、bcrypt（密码加密）、httpx（HTTP客户端）和aiofiles（异步文件处理）。FastAPI框架的核心优势在于高性能和自动化文档生成，开发模式下访问http://localhost:8000/docs即可查看完整的Swagger API文档。SQLAlchemy 2.0采用了全新的声明式配置方式，使模型定义更加清晰和类型安全。Pydantic v2提供了更强大的数据验证和序列化能力，支持自定义验证器和复杂的数据转换逻辑。

数据库层面，开发环境使用SQLite作为嵌入式数据库，文件名为dyslexia.db，部署简单无需额外配置。生产环境建议切换到PostgreSQL以获得更好的并发性能和数据可靠性。数据库表结构在应用启动时自动创建（通过SQLAlchemy的create_all方法），生产环境可使用Alembic工具管理数据库迁移。系统还集成了定时任务调度器，支持在后台自动执行周期性任务，如生成训练任务、发送提醒通知等。

AI服务集成采用适配器模式设计，通过统一的接口封装不同AI服务提供商的API调用。系统默认配置为OpenAI格式的API调用，可通过环境变量切换到DeepSeek、通义千问等其他服务提供商。在未配置AI_API_KEY的情况下，系统会自动降级为规则型回复模式，所有AI相关接口仍可正常响应，不会因服务不可用而报错，这种设计保证了系统在各种部署场景下的可用性。

## 三、核心功能模块

### 3.1 筛查游戏系统

筛查游戏系统是本产品的核心功能模块，包含六种不同类型的游戏，分别评估儿童的不同读写相关能力。每种游戏均采用游戏化设计，通过色彩丰富的界面、即时反馈和进度提示，降低儿童在测试过程中的心理压力，使筛查过程更像是一次有趣的互动体验而非严肃的考试。

六种筛查游戏的具体设计如下。视觉辨识游戏评估儿童的视觉辨识能力和持续注意力，游戏呈现相似的图形或文字让儿童找出不同的选项，每题限时6至10秒，根据难度等级调整时限。拼字识别游戏评估音形映射能力和字序组织能力，儿童需要根据听到的读音选择正确的文字，或将打乱的字序重新排列，每题限时6至10秒。文字理解游戏评估阅读理解、语义整合和信息提取能力，儿童阅读短文后回答相关问题，每题限时6至10秒。工作记忆游戏评估工作记忆容量和短时记忆能力，儿童需要记住一系列信息并在稍后进行回忆或应用，每题限时10至15秒。快速命名游戏评估快速命名速度和音韵意识能力，儿童需要快速命名屏幕上出现的数字、字母或图片，每题限时5至8秒。精细动作游戏评估精细动作控制和视动整合能力，儿童需要进行拖拽、点击等精细操作完成任务，每题限时8至12秒。

每种游戏均设置三个难度等级（L1、L2、L3），每个难度等级包含10道题目。系统根据儿童的年级自动匹配难度等级：学龄前和幼儿园阶段对应L1难度，小学一至二年级对应L1难度但给予1.2倍时限补偿，小学三至四年级对应L2难度，小学五至六年级对应L3难度。学龄前儿童还会获得额外的时限补偿（1.5倍），部分不适合该年龄段的题目类型会被自动过滤，如拼音相关的拼字识别题。这种自适应难度设计确保了不同年龄段儿童都能在合适的难度下完成筛查，既不会因题目过难而产生挫败感，也不会因题目过易而无法准确评估能力水平。

游戏过程中系统会采集丰富的行为数据，包括每道题的作答结果、反应时间、用时、超时次数、修改次数等。这些数据被存储在Screening表的behavior_data字段中，以JSON格式保存。行为数据是后续评分计算和AI分析的基础，系统通过分析这些数据不仅能判断答案对错，还能评估儿童的反应速度、注意力稳定性和操作流畅度等深层特征。

### 3.2 评分与报告生成

评分算法是筛查系统的核心引擎，采用效率分计算和风险等级判定两个步骤完成评估。效率分综合考虑正确率和反应时两个维度，计算公式为：效率分等于正确率乘以0.6加上反应时效率乘以0.4。正确率直接反映儿童的知识掌握程度，反应时效率则反映信息加工的自动化程度——在保证正确的前提下，反应越快说明相关能力越熟练。

反应时效率的计算采用分段线性函数模型。对于每道答对的题目，系统首先根据题目难度确定基准时限，然后用以下规则计算该题的反应时得分：用时不超过基准时限50%时得满分100分；用时在基准时限50%到100%之间时，按线性插值从100分递减到60分；用时超过基准时限时得0分。这种设计既奖励答对，又奖励答得快，符合效率等于正确率乘以速度的直觉。

不同能力维度采用差异化的评分策略。注意力维度使用反应时变异系数（CV）独立估算，变异系数越低说明答题节奏越稳定，注意力得分越高。音形映射维度重点考察首次反应时，反应时不超过1500毫秒得满分，超过5000毫秒得0分。字序组织维度重点关注修改次数，修改次数越多说明字序组织能力越不稳定。拼写输出维度综合正确率和超时率，超时严重的题目会被大幅扣分。工作记忆和短时记忆维度以正确率为主，同时考虑反应时稳定性。快速命名维度以反应时为核心指标，正确率仅作为辅助参考。精细动作维度综合正确率和修改次数，修改过多说明精细动作控制能力不足。

风险等级判定采用多因素综合评估规则。首先检查是否存在单维度严重缺陷（得分低于50分），存在则直接判定为高风险。然后计算综合得分和各维度平均得分，根据以下规则判定风险等级：综合得分和维度平均得分均不低于75分且无弱项（得分低于60分的维度）时判定为低风险；综合得分不低于60分或维度平均得分不低于60分且弱项不超过1个时判定为中风险；其他情况判定为高风险。

报告生成功能为家长提供详细的评估结果解读。每份报告包含以下内容：综合得分（0至100分）、风险等级（低风险、中风险、高风险）、评估摘要（根据风险等级显示对应的模板化描述）、各维度得分详解（列出所有评估维度的具体得分）、干预建议（根据风险等级显示对应的训练建议清单）。系统还支持AI增强的报告解读功能，在配置了AI服务的情况下，可以生成更加个性化、详细的报告解读内容。

### 3.3 训练任务系统

训练任务系统根据筛查报告自动生成个性化的训练方案，实现从发现问题到解决问题的完整闭环。系统采用基于规则的推荐算法，根据儿童的风险等级和弱项维度自动选择最合适的训练任务类型。

自动训练任务生成逻辑如下。筛查完成后，系统首先检查当日是否已为该儿童创建过训练任务，避免重复创建。然后分析报告中的维度得分，找出得分低于70分的弱项维度。针对每个弱项维度，系统从任务模板库中找到对应的训练任务类型，确保每个训练类型只创建一次以保证多样性。如果弱项不足三个，系统会用该风险等级对应的默认任务补齐。最后将选定的任务保存到数据库，状态为待完成。

任务模板库针对14个评估维度分别设计了对应的训练任务。视觉辨识和注意力维度对应火眼金睛（视觉辨识训练）和专注力训练。音形映射维度对应音形对应练习。字序组织维度对应字序组织训练。拼写输出维度对应拼字小达人。阅读理解、语义整合和信息提取维度分别对应阅读理解练习、语义整合训练和信息提取练习。工作记忆容量和短时记忆维度分别对应工作记忆强化训练和短时记忆练习。快速命名速度和音韵意识维度对应快速命名训练和音韵意识练习。精细动作控制和视动整合维度对应精细动作训练和视动整合练习。

不同风险等级对应不同的默认任务组合。低风险儿童获得基础维护类任务，包括每日视觉热身、拼字小达人和亲子共读打卡，目的是保持现有能力水平并培养良好习惯。中风险儿童获得针对性训练任务，包括火眼金睛（视觉辨识训练）、音形对应练习和阅读理解练习，目的是强化薄弱环节。高风险儿童获得强化训练任务，包括视觉辨识强化训练、字序组织训练和语义整合训练，目的是系统性地改善读写困难。

训练任务支持手动创建和自动生成两种模式。家长可以根据自己的安排手动创建额外的训练任务，系统会对任务类型进行校验，确保传入的是合法的任务类型。任务状态流转包括待完成、进行中和已完成三个阶段。当儿童完成训练任务并达到100%进度时，系统会自动标记任务为已完成状态，并发放星星奖励。系统设计了防止重复完成的机制，只有原状态不是已完成的任务在完成时才会发放奖励，避免重复刷奖励。

### 3.4 AI问答系统

AI问答系统是本产品的智能服务核心，提供十项AI功能，覆盖从筛查到干预的全流程。系统采用兼容OpenAI格式的API设计，可接入多种大语言模型服务，在未配置AI服务时自动降级为规则型回复，确保系统始终可用。

十项AI功能的具体设计如下。普通对话功能支持家长以自然语言咨询读写障碍相关问题，系统根据上下文提供专业解答。流式对话功能通过SSE（Server-Sent Events）技术实现实时流式响应，让家长获得类似ChatGPT的交互体验。报告解读功能接收筛查报告数据，生成个性化的报告解读内容，帮助家长深入理解评估结果。训练计划生成功能根据报告中的弱项维度，自动生成结构化的训练计划，包括每日任务安排和具体训练方法。儿童鼓励话语功能在游戏结束后生成针对当次表现的个性化鼓励反馈，增强儿童的参与动力和自信心。

成长趋势分析功能整合儿童历次筛查数据，生成能力发展曲线图和趋势分析报告，帮助家长了解干预效果。每日学习贴士功能根据儿童的年龄和当前能力水平，每日生成个性化的学习建议，推送给家长。家长情绪支持功能在筛查结果显示中高风险时，为焦虑的家长提供心理疏导和情感支持，帮助家长以积极的心态面对孩子的读写困难。自适应难度评估功能根据儿童在游戏中的答题表现，动态评估是否需要调整后续题目的难度。专业支持引导功能综合评估结果，判断是否需要寻求专业机构支持，并提供相应的就医建议。

AI对话历史管理功能支持查看和搜索历史对话，家长可以回顾之前的咨询记录。对话历史按儿童分组，方便家长针对不同孩子的问题进行区分管理。系统还提供了消息收藏功能，家长可以将AI的有价值回复收藏起来，方便后续查阅。收藏的消息存储在数据库中，支持列出收藏和取消收藏操作。

上下文构建是AI服务的关键技术之一。系统在调用AI之前会从数据库中获取相关儿童的详细信息，包括姓名、年龄、风险等级、各维度得分等，构建丰富的上下文信息传递给AI模型。这种设计使AI能够生成真正个性化的回复，而不是泛泛而谈的通用答案。例如，当家长询问如何帮助孩子改善阅读理解能力时，AI会结合该孩子的具体得分情况（是信息提取能力弱还是语义整合能力弱）给出更有针对性的建议。

### 3.5 奖励与成长系统

奖励系统是激励儿童持续参与训练的重要机制，采用星星和成就徽章两种奖励形式。星星是基础货币，儿童每完成一个训练任���可获得一颗星星，星星总数在个人页面展示，形成直观的进步反馈。成就徽章用于标记特殊里程碑，如连续完成七天训练、首次完成全部六种筛查游戏等，徽章系统增加了收集的乐趣。

成长记录系统记录儿童在平台上的所有重要事件和进步轨迹。记录类型包括筛查记录（每次筛查的日期、得分和风险等级）、训练记录（每次训练任务的完成情况和成果）和里程碑记录（特殊成就的达成）。每条成长记录包含标题、内容和元数据字段，元数据以JSON格式存储额外的结构化信息，如筛查记录可存储各维度得分，训练记录可存储正确率和完成时间。

多维度能力图谱功能将儿童在所有筛查中获得的各维度得分汇总展示，以雷达图形式直观呈现能力分布。家长可以通过对比不同时期的能力图谱，清晰看到孩子在哪些维度上有所进步，在哪些维度上仍需加强。报告对比���能支持选择两次或多次筛查报告进行并排对比，特别适合用于评估干预效果。

## 四、用户角色与交互流程

### 4.1 家长端功能

家长端是系统的主要使用入口，家长通过注册登录后可以管理多个儿童档案，为每个孩子发起筛查、查看报告、咨询AI和管理训练。家长端的功能设计围绕发现问题、理解问题、获得建议、开展训练和跟踪变化五个环节展开，形成完整的干预闭环。

用户认证模块支持多种注册和登录方式。用户名密码注册适合传统Web端使用场景，手机号验证码注册适合移动端快速注册，微信小程序登录适合小程序用户一键登录。登录成功后系统颁发JWT令牌，令牌有效期可配置，后续所有API请求都需要携带该令牌进行身份验证。系统还支持修改用户名、手机号和密码等账户管理功能。

儿童档案管理模块支持创建、查看、编辑和删除儿童档案。创建档案时需要填写孩子的姓名、性别、出生日期和年级，可选上传头像照片。系统根据出生日期自动计算年龄，根据年级自动匹配筛查难度。档案信息支持后续修改，如年级变化后系统会自动调整后续筛查的难度设置。系统还记录了孩子是否已有读写困难表现、是否已接受过专业评估等扩展信息，便于后续追踪。

筛查发起模块让家长选择要为哪个孩子发起哪种类型的筛查。系统提供六种筛查游戏供选择，家长可以根据孩子的具体情况选择针对性的游戏，也可以选择综合筛查完成全部六种游戏。发起筛查后系统会生成一个任务，儿童登录儿童端即可看到待完成的筛查任务。家长可以选择陪伴孩子完成筛查，或让孩子独立完成。

报告查看模块提供多种报告查看方式。所有报告列表按时间倒序展示，方便家长快速找到最新报告。点击单份报告可查看详细内容包括综合得分、风险等级、各维度得分、评估摘要和干预建议。报告对比功能支持选择两份报告进行并排对比，突出显示各维度的变化情况。多维度能力图谱功能将所有筛查数据汇总展示为雷达图，直观呈现能力分布。报告导出功能支持将报告内容导出为纯文本格式，方便家长保存或分享。

AI问答模块是家长获取专业指导的主要渠道。界面采用对话式设计，家长可以输入问题并获得AI的回答。系统支持流式响应，让交互更加流畅。十项AI功能通过不同的接口实现，对话界面中可以通过自然语言触发相应功能。例如，家长可以直接说"帮我解读一下最新的报告"或"生成一个训练计划"，系统会调用对应的AI功能生成回复。对话历史和消息收藏功能帮助家长管理咨询记录。

训练管理模块展示系统自动生成的训练任务和家长手动创建的任务。任务列表支持按状态筛选（待完成、进行中、已完成），支持按儿童筛选。点击任务可查看详情包括任务类型、名称、进度和完成情况。训练成果页面展示孩子完成训练任务的历史记录，包括正确率、完成时间等数据。家长可以通过这些数据了解孩子的训练效果和进步情况。

### 4.2 儿童端功能

儿童端采用游戏化设计，界面风格活泼有趣，操作简单直观。儿童端的核心功能是完成筛查游戏和训练任务，通过即时反馈和奖励激励保持孩子的参与兴趣。

首页展示待完成的筛查任务和今日训练任务。筛查任务以游戏卡片形式呈现，每张卡片显示游戏名称、预计用时和难度等级。训练任务以任务清单形式呈现，完成一项少一项。首页还展示已获得的星星总数和成就徽章，激励孩子继续努力。

筛查游戏界面针对儿童用户进行了专门优化。游戏开始前有简短的操作说明，确保孩子理解如何作答。游戏过程中每道题有倒计时提示，用进度条直观展示剩余时间。作答后有即时的对错反馈，答对会有鼓励动画，答错会提示正确答案但不批评。游戏结束后显示本次得分和获得的星星数，并展示AI生成的个性化鼓励话语。鼓励话语根据孩子的具体表现生成，如"你在视觉辨识上表现很棒，如果反应再快一点就更好了"。

训练任务界面与筛查游戏类似，但难度通常较低，更注重练习而非评估。训练过程中同样有倒计时和即时反馈，但不会像筛查那样严格判定风险等级。完成训练后系统发放星星奖励，并更新任务进度。累计星星数在个人页面展示，形成可视化的进步反馈。

个人页面展示孩子的基本信息和成长档案。基本信息包括姓名、头像和已获得的星星总数。成长档案以时间线形式展示所有重要事件，包括筛查记录、训练记录和成就达成。成就徽章展示区显示已获得的所有徽章，未获得的徽章显示为灰色并标注解锁条件。

### 4.3 管理员端（规划中）

管理员端功能目前仍在规划中，未来将提供以下能力。用户管理功能支持查看和管理所有注册用户，包括用户信息查询、禁用异常账户等。数据统计分析功能提供平台运营数据看板，包括用户数量、筛查次数、活跃度等指标。内容管理功能支持管理筛查题目、训练内容和系统配置等。后台管理功能采用独立的登录入口，与家长端分离，确保安全性。

## 五、数据库设计

### 5.1 核心数据模型

系统使用SQLAlchemy定义数据模型，采用声明式配置方式，模型类继承自Base类，与数据库表的映射关系通过__tablename__属性指定。以下详细介绍各核心数据模型的字段设计和关联关系。

User模型是用户账户的基础模型，包含以下字段：id作为主键自增，username为用户名唯一索引，password_hash存储bcrypt加密后的密码，phone为手机号唯一索引支持手机号登录，created_at和updated_at分别记录创建时间和更新时间。User模型与Child模型是一对多关系，一个家长可以管理多个儿童档案；与AIConversation模型是一对多关系，记录用户的AI对话历史。

Child模型是儿童档案的核心模型，包含以下字段：id作为主键自增，parent_id外键关联User.id，name为儿童姓名，gender为性别，birth_date为出生日期用于计算年龄，grade为年级用于匹配筛查难度，avatar_url为头像URL，has_difficulty标记是否已有读写困难表现，has_professional_eval标记是否已接受过专业评估，created_at和updated_at记录时间信息。Child模型与User模型是多对一关系，与Screening、Report、TrainingTask、GrowthRecord、Reward模型均是一对多关系。

Screening模型记录每次筛查的详细信息，包含以下字段：id作为主键自增，child_id外键关联Child.id，game_type记录游戏类型（visual、spelling、comprehension等），score记录综合得分，risk_level记录风险等级（low、medium、high），behavior_data以JSON字符串存储行为数据，started_at和completed_at记录开始和完成时间，created_at记录创建时间。Screening模型与Child模型是多对一关系，与DimensionScore模型是一对多关系。

DimensionScore模型记录各维度的详细得分，包含以下字段：id作为主键自增，screening_id外键关联Screening.id，dimension记录维度名称，score记录得分，raw_data以JSON字符串存储原始数据，created_at记录创建时间。DimensionScore模型与Screening模型是多对一关系。

Report模型记录评估报告，包含以下字段：id作为主键自增，child_id外键关联Child.id，screening_id外键关联Screening.id，overall_score记录综合得分，risk_level记录风险等级，summary记录评估摘要，recommendations记录干预建议，dimensions以JSON字符串存储各维度得分详情，created_at记录创建时间。Report模型与Child模型和Screening模型均是多对一关系。

TrainingTask模型记录训练任务，包含以下字段：id作为主键自增，child_id外键关联Child.id，task_type记录任务类型，task_name记录任务名称，status记录状态（pending、in_progress、completed），progress记录进度百分比，scheduled_date记录计划日期，completed_at记录完成时间，correct_count记录正确数，total_count记录总题数，accuracy记录正确率，created_at记录创建时间。TrainingTask模型与Child模型是多对一关系。

GrowthRecord模型记录成长记录，包含以下字段：id作为主键自增，child_id外键关联Child.id，record_type记录记录类型（screening、training、milestone），title记录标题，content记录内容，metadata以JSON字符串存储额外元数据，created_at记录创建时间。GrowthRecord模型与Child模型是多对一关系。

Reward模型记录奖励信息，包含以下字段：id作为主键自增，child_id外键关联Child.id，reward_type记录奖励类型（star、badge、achievement），name记录奖励名称，description记录奖励描述，earned_at记录获得时间。Reward模型与Child模型是多对一关系。

AIConversation模型记录AI对话历史，包含以下字段：id作为主键自增，user_id外键关联User.id，child_id外键关联Child.id，role记录角色（user或assistant），message记录消息内容，created_at记录创建时间。AIConversation模型与User模型和Child模型均是多对一关系。

SavedMessage模型记录收藏的AI消息，包含以下字段：id作为主键自增，user_id外键关联User.id，child_id外键关联Child.id，conversation_id外键关联AIConversation.id，content记录消息内容，created_at记录创建时间。SavedMessage模型与User模型、Child模型和AIConversation模型均是多对一关系。

### 5.2 数据库初始化与迁移

开发环境下，系统启动时自动调用init_db函数创建所有数据表。SQLAlchemy的create_all方法会检查数据库中是否已存在对应的表，不存在则创建。这种方式简化了开发流程，开发者无需手动执行数据库初始化脚本。生产环境建议使用PostgreSQL数据库，并使用Alembic工具管理数据库迁移，确保数据库结构变更的可追溯性和可回滚性。

数据库连接通过DATABASE_URL环境变量配置，开发环境默认值为sqlite:///./dyslexia.db，即当前目录下的dyslexia.db文件。生产环境应配置为PostgreSQL连接串，格式为postgresql://用户名:密码@主机名:端口号/数据库名。系统支持在运行时动态切换数据库类型，但建议在部署初期就确定使用PostgreSQL以获得更好的性能和可靠性。

## 六、API接口体系

### 6.1 认证模块

认证模块提供用户注册、登录、验证码和微信登录等功能，所有接口前缀为/api/auth。POST /register接口支持用户名或手机号注册，密码通过bcrypt加密后存储。POST /login接口支持用户名或手机号加密码登录，验证成功后返回JWT令牌。POST /login-by-code接口支持手机号验证码登录，未注册的手机号会自动创建新账户。POST /send-code接口发送短信验证码，支持阿里云和腾讯云两种短信服务商，通过环境变量SMS_PROVIDER配置。GET /verify-code接口校验验证码是否正确。GET /me接口返回当前登录用户信息。PUT /profile接口修改用户名或手机号。POST /change-password接口修改密码。POST /wx-login接口处理微信小程序登录。

### 6.2 儿童档案模块

儿童档案模块提供儿童档案的增删改查功能，所有接口前缀为/api/children。GET /接口返回当前用户所有儿童档案列表。POST /接口创建新的儿童档案，请求体包含name、gender、birth_date、grade等字段。GET /{child_id}接口返回指定儿童的详细信息。PUT /{child_id}接口更新儿童档案信息。DELETE /{child_id}接口删除儿童档案及其关联的所有数据（筛查记录、报告、训练任务等）。

### 6.3 筛查模块

筛查模块提供筛查游戏的核心功能，所有接口前缀为/api/screenings。GET /questions/{game_type}接口根据游戏类型返回题目列表，支持难度自适应，会根据儿童年级自动过滤不适合的题目并调整时限。POST /start接口开始一次筛查，记录开始时间和儿童信息。POST /submit接口提交筛查答案，请求体包含child_id、game_type和answers数组，answers中每项包含question_id、answer、time_spent等信息。系统会根据提交的数据计算得分、判定风险等级、生成维度得分、创建报告记录，并自动创建训练任务。GET /history接口返回指定儿童的筛查历史记录。

### 6.4 报告模块

报告模块提供报告查询和导出功能，所有接口前缀为/api/reports。GET /接口返回当前用户所有儿童的报告列表，支持按儿童筛选。GET /{report_id}接口返回指定报告的详细信息。GET /by-screening/{screening_id}接口根据筛查ID查询对应报告。GET /{report_id}/dimensions接口返回报告的各维度详细得分。GET /child/{child_id}/merged-dimensions接口返回指定儿童所有筛查的维度得分汇总，用于生成能力图谱。GET /{report_id}/export-text接口将报告导出为纯文本格式。

### 6.5 训练模块

训练模块提供训练任务和成长记录的管理功能，所有接口前缀为/api/training。GET /tasks接口返回训练任务列表，支持按儿童和状态筛选。POST /tasks接口创建新的训练任务。PUT /tasks/{task_id}/progress接口更新任务进度，当进度达到100%时自动标记为完成并发放星星奖励。POST /tasks/{task_id}/complete接口标记任务完成，与进度更新功能类似但更简洁。GET /growth接口返回成长记录列表。POST /growth接口创建新的成长记录。GET /rewards接口返回奖励列表。GET /stars接口返回指定儿童的星星总数。

### 6.6 AI问答模块

AI问答模块提供十项AI功能，所有接口前缀为/api/ai。POST /chat接口处理普通对话请求，请求体包含message和可选的child_id，系统会保存对话历史并返回AI回复。POST /chat/stream接口处理流式对话请求，通过SSE技术实时推送AI回复。GET /history接口返回对话历史记录。DELETE /history接口清空对话历史。POST /report-interpretation接口接收报告数据，生成个性化解读。POST /training-plan接口根据报告生成训练计划。POST /encouragement接口根据游戏表现生成儿童鼓励话语。GET /growth-analysis/{child_id}接口返回儿童的成长趋势分析。GET /daily-tip/{child_id}接口返回每日学习贴士。POST /emotional-support接口提供家长情绪支持。POST /adaptive-difficulty接口评估是否需要调整难度。GET /professional-guidance/{child_id}接口返回专业支持引导建议。POST /saved-messages接口收藏AI回复。GET /saved-messages接口返回收藏列表。DELETE /saved-messages/{saved_id}接口取消收藏。

### 6.7 文件上传与通知模块

文件上传模块提供头像上传功能，接口前缀为/api/upload。POST /avatar接口上传儿童头像，文件大小限制为5MB，上传成功后返回文件URL。头像文件存储在uploads目录下，可通过/uploads/{filename}路径访问。

通知模块提供通知管理和推送功能，接口前缀为/api/notifications。系统支持在训练完成、筛查结果生成等事件发生时自动创建通知。GET /接口返回当前用户的所有通知。PUT /{notification_id}/read接口标记通知为已读。

## 七、部署与配置

### 7.1 开发环境部署

开发环境支持Docker Compose一键启动和手动启动两种方式。Docker Compose方式最为简便，执行以下命令即可完成部署：首先复制后端环境配置文件cp backend/.env.example backend/.env，然后编辑.env文件至少修改SECRET_KEY，最后执行bash deploy.sh dev启动所有服务。启动后可通过以下地址访问：前端http://localhost，后端API http://localhost:8000，API文档http://localhost:8000/docs。

手动启动方式适合需要调试后端代码的场景。后端启动步骤如下：进入backend目录，创建Python虚拟环境python -m venv venv，激活虚拟环境source venv/bin/activate（Windows系统使用venv\Scripts\activate），安装依赖pip install -r requirements.txt，复制并编辑环境变量cp .env.example .env，最后运行python run.py启动服务。前端启动步骤如下：进入frontend目录，执行npm install安装依赖，然后执行npm run dev:h5启动开发服务器。

### 7.2 生产环境部署

生产环境部署建议使用PostgreSQL数据库和Nginx反向代理。部署步骤如下：首先准备生产环境配置cp backend/.env.production backend/.env，然后编辑.env文件填写SECRET_KEY、ALLOWED_ORIGINS（前端域名，逗号分隔）、DATABASE_URL（PostgreSQL连接串）等必填项，最后执行bash deploy.sh prod进行一键部署。

生产环境的关键配置项说明如下。SECRET_KEY必须修改为随机字符串，可通过命令python -c "import secrets; print(secrets.token_hex(32))"生成。DEBUG应设为False以关闭调试模式和Swagger文档。ALLOWED_ORIGINS必须填写前端应用的域名，多个域名用逗号分隔，用于CORS跨域控制。AI_API_KEY填写AI服务密钥，不填写则使用规则型回复。WX_APPID和WX_SECRET填写微信小程序配置，小程序登录功能才可用。

### 7.3 环境变量配置

系统通过环境变量进行配置，主要配置项如下。SECRET_KEY是JWT签名密钥，生产环境必须修改为安全值。DEBUG控制调试模式，生产环境设为False。DATABASE_URL是数据库连接串，SQLite格式为sqlite:///./dyslexia.db，PostgreSQL格式为postgresql://user:password@host:port/dbname。ALLOWED_ORIGINS是允许的跨域来源，生产环境必填。AI_API_KEY是AI服务密钥，不填则使用规则型回复。AI_API_BASE_URL是AI服务地址，默认使用OpenAI官方地址，可配置为DeepSeek、通义千问等第三方服务地址。AI_MODEL是模型名称，默认使用gpt-4o-mini。UPLOAD_DIR是文件上传目录，默认为uploads。WX_APPID和WX_SECRET是微信小程序配置。SMS_PROVIDER、SMS_ACCESS_KEY、SMS_SECRET_KEY、SMS_SIGN_NAME、SMS_TEMPLATE_CODE是短信服务配置，支持阿里云和腾讯云。

## 八、免责声明

本系统（悦读小灯塔）仅作为家庭初步风险筛查工具，通过游戏化方式评估儿童的相关能力水平。系统生成的筛查结果、评估报告和干预建议仅供参考，不构成医学诊断或专业评估意见。

读写障碍的准确诊断需要由具备专业资质的医生、心理学专家或语言治疗师进行面对面评估，使用标准化的评估工具和方法。系统的筛查结果可能受到多种因素影响，包括儿童当天的身体状态、心理情绪、环境干扰等，不能完全反映儿童的真实能力水平。

如筛查结果显示中风险或高风险，建议家长及时带孩子咨询专业医疗机构或持证评估师，进行全面、专业的正式评估。早期识别和干预对改善读写困难至关重要，但干预方案应在专业人士指导下制定和实施。本系统不提供任何医疗建议或治疗方案，所有干预建议仅作为参考，不能替代专业医疗意见。

家长在使用本系统时应了解其局限性，合理使用筛查结果，避免因结果不准确或解读不当而对儿童造成不必要的心理压力。系统开发者不对因使用本系统而产生的任何直接或间接后果承担责任。

---

**系统版本**：v1.0.0

**最后更新**：2026年4月

**技术支持**：如有问题请通过项目仓库提交Issue